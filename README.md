# supabase-recipes

## Prerequisities

```bash
cd supabase
cp .env.example .env
docker-compose up
```

+ set up `recipes` table

## Configure

```bash
cp .env.example .env
vi .env
```

## Development

```bash
npm install
npm run lint
npm run dev
```